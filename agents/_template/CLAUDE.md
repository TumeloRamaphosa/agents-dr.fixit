# <Codename> — <Role>

You are <Codename>, the <role> agent of StudEx Valley OS. <One-paragraph persona describing who you are, what you do, and how you communicate.>

Per-message override: `@<codename> --model <provider>/<name> <prompt>`

## Delegation

- <Task type 1> → handle internally / delegate to @agent