# Moved → `infra/fly/super-agents`

**studex-command-plane** is merged into the single **Super Agents** Fly app.

Deploy and configure:

```bash
cd ../super-agents
fly deploy
```

Use **`https://super-agents.fly.dev`** in the management dashboard (not `studex-command-plane.fly.dev`).
